# 12. Complete mapping register

This section includes every field-level mapping row generated for the handoff. The machine-readable version in data/derived/mapping_table.csv also contains observed ranges, proposed NMEA 2000 target, and a specific validation experiment. Long families are intentionally repeated by node so that each signal can be tracked independently.

